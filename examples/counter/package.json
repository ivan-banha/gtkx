{
    "name": "counter-example",
    "version": "0.0.0",
    "private": true,
    "description": "Counter example - A simple counter app demonstrating state and testing",
    "type": "module",
    "scripts": {
        "build": "tsc -b",
        "start": "node dist/index.js",
        "test": "if [ -n \"$CI\" ]; then vitest run; else GDK_BACKEND=x11 xvfb-run -a vitest run; fi"
    },
    "dependencies": {
        "@gtkx/ffi": "workspace:*",
        "@gtkx/react": "workspace:*",
        "react": "19.2.0"
    },
    "devDependencies": {
        "@gtkx/testing": "workspace:*",
        "@types/react": "19.2.6",
        "vitest": "3.2.3"
    }
}
